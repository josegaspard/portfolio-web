{
  "name": "josegaspard-backend",
  "version": "1.0.0",
  "description": "Backend API for Jose Gaspard Portfolio",
  "main": "backend/dist/main.js",
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm run start:prod",
    "start:prod": "cd backend && node dist/main.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}