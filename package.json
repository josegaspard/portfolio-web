{
  "name": "josegaspard-portfolio",
  "version": "1.0.0",
  "description": "Full-stack portfolio - Frontend (Next.js) + Backend (NestJS)",
  "scripts": {
    "build": "chmod +x build.sh && ./build.sh",
    "start": "chmod +x start.sh && ./start.sh",
    "install-all": "cd backend && npm install && cd ../frontend-next && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}