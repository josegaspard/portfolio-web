/** @type {import('next').NextConfig} */
const nextConfig = {
    // This is a root config file to satisfy Hostinger's framework detector.
    // The actual application lives in /frontend-next
    reactStrictMode: true,
};

module.exports = nextConfig;
